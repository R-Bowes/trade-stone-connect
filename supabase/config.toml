project_id = "tnvxfzmdjpsswjszwbvf"

[auth]

[functions.send-quote-notification]
# JWT verification disabled - this function accepts anonymous quote submissions
# Security is enforced via: CORS origin validation, rate limiting, input validation
verify_jwt = false

[functions.notify-invoice-quote-action]
verify_jwt = false

[functions.create-payment-intent]
verify_jwt = false

[functions.stripe-webhook]
verify_jwt = false

[functions.mark-overdue-invoices]
verify_jwt = false

[functions.sla-clock]
# Internal/service-to-service — invoked on job create/approve and by a scheduled check, not by end users
verify_jwt = false

[functions.cert-expiry-check]
# Internal/service-to-service — cron-invoked only, not by end users
verify_jwt = false

[functions.insurance-expiry-check]
# Internal/service-to-service — cron-invoked only, not by end users
verify_jwt = false

[functions.evaluate-craft-timers]
# Internal/service-to-service — cron-invoked only, not by end users
verify_jwt = false

[functions.recalculate-scores]
# Internal/service-to-service — cron-invoked only, not by end users
verify_jwt = false

[functions.process-recurring-expenses]
# Internal/service-to-service — cron-invoked only, not by end users
verify_jwt = false

[functions.notify-overdue-visits]
# Internal/service-to-service — cron-invoked only, not by end users
verify_jwt = false

[functions.send-cooling-off-notice]
# Internal/service-to-service — invoked by mint_job_from_quote's net.http_post, not by end users
verify_jwt = false
